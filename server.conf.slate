include slate

! Slate Demo Config

while (slate) {
    new slateserver(DemoApp) {
        SRC=(/)
        PORT=(80)
        PORT_SECURE=(443)
        TEMPLATE=(index.html)
        STYLES=(css/styles.css)
        APP=(js/app.js)
        FAVICON=(favicon.ico)
    }
    return 0
}
